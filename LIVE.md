# Live model integration

LlmLang talks to any **OpenAI-compatible** Chat Completions API.

```bash
export OPENAI_API_KEY=sk-...
export OPENAI_MODEL=gpt-4o-mini
python -m llm_lang --live
```

See full docs in the repository for providers (Groq, Ollama, vLLM).
